import 'package:flutter/material.dart';

class OrderDishDetail extends StatelessWidget {
  const OrderDishDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      title: Text('Contact Us'),
      content: ListView(
        shrinkWrap: true,
        children: [

        ],
      )
    );
  }
}
